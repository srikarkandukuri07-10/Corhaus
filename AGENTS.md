<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Workflow preferences

- After completing a code change that passes typecheck/build, commit and push it immediately without being asked. Only stage the files you actually changed for that task; leave unrelated/untracked files (e.g. `.freebuff/`, `.gitignore`) alone unless asked. Use a concise commit message matching the repo style.

